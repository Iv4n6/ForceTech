RewriteEngine On

# If the request targets an existing file/directory, serve it directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route short codes like /Ab12Xy to api.php?c=Ab12Xy
RewriteRule ^([A-Za-z0-9]{6,8})$ api.php?c=$1 [L,QSA]
